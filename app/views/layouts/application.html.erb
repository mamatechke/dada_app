<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= content_for?(:title) ? yield(:title) : "Dada" %></title>

  <!-- PWA Meta Tags -->
  <meta name="application-name" content="DADA" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-status-bar-style" content="default" />
  <meta name="apple-mobile-web-app-title" content="DADA" />
  <meta name="description" content="Your culturally-rooted menopause companion" />
  <meta name="format-detection" content="telephone=no" />
  <meta name="mobile-web-app-capable" content="yes" />
  <meta name="theme-color" content="#C85A54" />

  <!-- PWA Manifest -->
  <link rel="manifest" href="/manifest.json" />
  <link rel="icon" type="image/png" href="/icon.png" />
  <link rel="apple-touch-icon" href="/icon.png" />

  <!-- Use prebuilt Tailwind CSS from app/assets/builds instead of the CDN in production -->
  <%= stylesheet_link_tag "tailwind", data: { turbo_track: "reload" } %>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Service Worker Registration -->
  <script>
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', () => {
        navigator.serviceWorker.register('/sw.js')
          .then((reg) => console.log('Service Worker registered'))
          .catch((err) => console.log('Service Worker registration failed'));
      });
    }
  </script>
</head>

<body class="bg-dada-background text-dada-text dark:bg-dada-darkBg dark:text-dada-background transition-colors duration-300 min-h-screen flex flex-col">
<%= render "shared/navbar" %>
<!-- ✅ MAIN CONTENT -->
<main class="flex-1">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
    <%= yield %>
  </div>
</main>
<%= render "shared/footer" %>

</body>
</html>
